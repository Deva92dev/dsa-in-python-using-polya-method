# The Problem:You are given an array of integers. Some of these integers are 0.Your goal is to move all the 0s to the very end of the array, while maintaining the exact relative order of all the non-zero elements.

# Example of the desired outcome:If memory starts as: [0, 1, 0, 3, 12]Memory must end as: [1, 3, 12, 0, 0]

# Strict Constraints:Space Complexity: Strictly $O(1)$. (Definition: You may use a few individual variables for pointers or temporary holds, but you cannot create a second array/list. You must mutate the existing array in-place).Operations: You must minimize the total number of operations.

print("Hello")
